name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Inject secrets
        env:
          GUESTBOOK_TOKEN: ${{ secrets.GUESTBOOK_TOKEN }}
          GUESTBOOK_OWNER: ${{ secrets.GUESTBOOK_OWNER }}
          GUESTBOOK_REPO: ${{ secrets.GUESTBOOK_REPO }}
        run: |
          sed -i "s/YOUR_FINE_GRAINED_TOKEN/${GUESTBOOK_TOKEN}/g" index.html form.html entry.html
          sed -i "s/GUESTBOOK_OWNER_PLACEHOLDER/${GUESTBOOK_OWNER}/g" index.html form.html entry.html
          sed -i "s/GUESTBOOK_REPO_PLACEHOLDER/${GUESTBOOK_REPO}/g" index.html form.html entry.html

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - uses: actions/deploy-pages@v4
        id: deployment
